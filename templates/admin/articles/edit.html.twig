{% extends 'admin/base.html.twig' %}

{% block title %}Modification de l'article{% endblock %}

{% block body %}    
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Modification de l'article</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
          <div class="btn-group me-2">
              <a href="{{ path('admin_articles') }}" class="link-info"><< Revenir à la liste des articles</a>
          </div>
        </div>
      </div>

      {{ include('admin/_partials/_article_form.html.twig', {'button_label': 'Mettre à jour'}) }}
      {{ include('admin/_partials/_article_delete_form.html.twig') }}
{% endblock %}