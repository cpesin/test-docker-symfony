{% extends 'admin/base.html.twig' %}

{% block title %}Ajouter un nouvel auteur{% endblock %}

{% block body %}    
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Ajouter un nouvel auteur</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
          <div class="btn-group me-2">
              <a href="{{ path('admin_authors') }}" class="link-info"><< Revenir à la liste des auteurs</a>
          </div>
        </div>
      </div>

      {{ include('admin/_partials/_author_form.html.twig', {'button_label': 'Enregistrer'}) }}
{% endblock %}